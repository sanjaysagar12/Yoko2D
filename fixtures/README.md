# Fixtures

This directory will hold sample pattern files (`.val`/`.vit`/`.smis` or
whatever format `io` ends up supporting) and measurement files used as test
fixtures in later phases, once file reading/writing is implemented in the
`io` crate. It is currently empty — Phase 0 is scaffolding only.
